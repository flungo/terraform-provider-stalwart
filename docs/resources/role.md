---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stalwart_role Resource - stalwart"
subcategory: ""
description: |-
  Manages a Stalwart role: a named set of permissions (the Role JMAP object).
---

# stalwart_role (Resource)

Manages a Stalwart role: a named set of permissions (the `Role` JMAP object).

## Example Usage

```terraform
resource "stalwart_role" "support" {
  description          = "Support team role"
  enabled_permissions  = ["emailSend", "emailReceive"]
  disabled_permissions = ["impersonate"]
}

# Import an existing role by its description:
# terraform import stalwart_role.support "Support team role"
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `description` (String) Description of the role. This is the role's human-facing identifier.

### Optional

- `disabled_permissions` (Set of String) Permissions explicitly disabled by this role; takes precedence over enabled permissions.
- `enabled_permissions` (Set of String) Permissions explicitly enabled by this role.
- `extends` (Set of String) Ids of roles this role extends (maps to `roleIds`).

### Read-Only

- `id` (String) Opaque server-assigned identifier of the role.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import by role description (preferred):
terraform import stalwart_role.support "Support team role"

# Import by opaque server-assigned id:
# terraform import stalwart_role.support <opaque-id>
```
